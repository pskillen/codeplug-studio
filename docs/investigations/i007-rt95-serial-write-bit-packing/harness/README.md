# Harness — Studio encode bytes

Scratch printer for `R/studio-encode`. Not a unit test.

From repo root:

```bash
npx vite-node --script docs/investigations/i007-rt95-serial-write-bit-packing/harness/encode-studio-bytes.mts
```

Do not commit live radio dumps next to this script.
