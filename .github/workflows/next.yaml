name: Deploy next

on:
  push:
    branches:
      - main

jobs:
  deploy:
    uses: ./.github/workflows/cloudflare-pages.yaml
    secrets: inherit
    with:
      build_env: next
      cloudflare_branch: main
