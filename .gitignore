# Private operator codeplugs (shared samples live in sample-exports/)
fixtures/
!cps-verify/fixtures/
!cps-verify/fixtures/**
local/

# Secrets and local env
.env
.env.*
!.env.example

# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor / IDE (local overrides only — repo keeps .cursor/ rules)
.idea/
*.swp
*.swo
*~

# Logs and temp
*.log
tmp/

# Node / Vite build output
node_modules/
dist/
coverage/
test-results/
*.tsbuildinfo

# Operator CPS exports for local testing
sample-exports/

# Codegen output
*.generated.ts

# Local MCP secrets (never commit)
.cursor/mcp.json

# Python dev-tools (each tool keeps its own venv; see dev-tools/README.md)
dev-tools/**/.venv/
dev-tools/**/__pycache__/
dev-tools/**/*.pyc
dev-tools/**/*.egg-info/
dev-tools/**/.pytest_cache/
