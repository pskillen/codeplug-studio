/** Settle time before list name search updates URL, storage, and table filtering. */
export const LIST_NAME_FILTER_DEBOUNCE_MS = 300;

export const LIST_ENTITY_LABELS: Record<string, string> = {
  channels: 'channels',
  zones: 'zones',
  'talk-groups': 'talk groups',
  'digital-contacts': 'digital contacts',
  'analog-contacts': 'analog contacts',
  'rx-group-lists': 'RX group lists',
};
