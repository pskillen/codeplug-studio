/**
 * @deprecated Import from `@core/models/radioBuild.ts` — this module re-exports
 * shared override/settings types and the RadioBuild rename (#654).
 */
export type {
  BuildEntityOverride,
  BuildExportSettings,
  DefaultScanInclusion,
  RadioBuild,
} from './radioBuild.ts';

/** @deprecated Use {@link RadioBuild}. */
export type { RadioBuild as FormatBuild } from './radioBuild.ts';
