import FeaturePlaceholder from './FeaturePlaceholder.tsx';

export default function ReportsPage() {
  return (
    <FeaturePlaceholder
      title="Reports"
      ticket="ticket #12"
      description="Summary views and integrity checks over the active project's library."
    />
  );
}
