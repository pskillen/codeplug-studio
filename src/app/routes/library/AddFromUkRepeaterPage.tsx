import RepeaterDirectorySearch from '../../components/repeaters/RepeaterDirectorySearch.tsx';

export default function AddFromUkRepeaterPage() {
  return (
    <RepeaterDirectorySearch
      source="ukrepeater"
      title="Add channel from ukrepeater.net"
      description="Search the RSGB ETCC repeater directory and add listings to your library as vendor-neutral channels."
    />
  );
}
