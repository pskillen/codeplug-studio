import FeaturePlaceholder from './FeaturePlaceholder.tsx';

export default function LibraryPage() {
  return (
    <FeaturePlaceholder
      title="Library"
      ticket="ticket #10"
      description="Curate channels, talk groups, contacts, RX group lists, and zones for the active project."
    />
  );
}
