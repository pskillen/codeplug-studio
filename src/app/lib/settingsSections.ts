/** PageSection id for Google Drive connect / disconnect. */
export const SETTINGS_DRIVE_SECTION_ID = 'settings-drive';

/** PageSection id for map preferences (grid overlay, geocode token). */
export const SETTINGS_MAP_SECTION_ID = 'settings-map';
